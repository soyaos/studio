// Stub. The streaming chat surface lands in the next commit
// (see `feat(chat): SSE streaming with markdown + tokens display`).
export default function Chat() {
  return (
    <div className="card p-6">
      <h1 className="text-2xl font-semibold tracking-tight">Chat</h1>
      <p className="mt-2 text-sm text-soya-muted">
        Streaming chat UI lands in the next commit.
      </p>
    </div>
  );
}
